@model IEnumerable<Forsoft2.Dominio.Usuario>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutListaUsuario.cshtml";
}

<p>
    @Html.ActionLink("Cadastrar Novos Usuários", "Cadastrar",null, new { @class = "btn_login" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idUsuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Senha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Permissao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Eventos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idUsuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Senha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Permissao)
        </td>
        <td>
            
            @*@{ 
                foreach (var item2 in item.Eventos)
                {
                    @Html.DisplayFor(ModelEvento => item2.Nome)
                }

            }*@
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.idUsuario}) |
            @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
            @Html.ActionLink("Excluir", "Excluir", new { id=item.idUsuario })
        </td>
    </tr>
}

</table>
